# Allen Bus Management - Python dependencies
# Recommended Python version: 3.11
#
# Notes for a fresh machine:
# - PostgreSQL client/dev tools are still required for `psycopg2-binary` usage in some environments.
# - QR / barcode related features may need `zbar` installed at the OS level.
# - PDF generation with `weasyprint` may need Cairo / Pango system libraries.
# - Dockerfile already installs several required OS packages for Linux containers.

# Core framework
Django==5.0.7
djangorestframework
django-admin-interface==0.29.2
django-colorfield==0.11.0
django-sslserver==0.22
django-celery-beat
django-grappelli

# App server / static serving
gunicorn==20.1.0
whitenoise==6.4.0

# Database / background jobs
psycopg2-binary
celery>=5.3
redis

# HTTP and utility libraries
requests==2.28.2
python-dateutil==2.9.0.post0

# Images / QR / computer vision
Pillow==10.4.0
qrcode==7.3.1
numpy==1.26.4
opencv-python-headless==4.10.0.84

# Maps and external service integrations
googlemaps
python-telegram-bot==20.7
Kashier==1.0.6

# AI integrations used in views
openai
google-generativeai

# PDF / reporting / Arabic text rendering
weasyprint
reportlab
arabic-reshaper
python-bidi

# Optional AI / vision stack kept from requirements_ai.txt
ultralytics
mediapipe
